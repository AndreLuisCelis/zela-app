# Configurações do Servidor
PORT=3000
NODE_ENV=development

# MongoDB Atlas
# Substitua os valores abaixo com suas credenciais do MongoDB Atlas
MONGO_URI=mongodb+srv://usuario:<PASSWORD>@<CLUSTER_URL>/zelamorato?retryWrites=true&w=majority

# Exemplo:
# MONGO_URI=mongodb+srv://zelauser:minhasenha123@cluster0.abc123.mongodb.net/zelamorato?retryWrites=true&w=majority

# JWT Authentication
# IMPORTANTE: Gere uma chave secreta forte e única para produção
# Você pode gerar uma com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua-chave-secreta-super-segura-aqui
JWT_EXPIRES_IN=7d
